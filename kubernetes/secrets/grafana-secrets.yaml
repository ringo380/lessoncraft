apiVersion: v1
kind: Secret
metadata:
  name: grafana-secrets
type: Opaque
data:
  # These are example values and should be replaced with secure values in production
  # The values are base64 encoded
  admin-user: YWRtaW4=  # "admin" in base64
  admin-password: YWRtaW4=  # "admin" in base64